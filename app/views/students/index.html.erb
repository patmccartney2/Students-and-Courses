<h1> All Students </h1>

<% @students.each do |s| %>

<ul>
  <li class=student_<%= s.id %>>
    <<%= link_to s.name, student_path(s) %>:
    <%= link_to "Delete", student_path(s.id), method: "delete" %>
  </li>
</ul>
<% end %>
